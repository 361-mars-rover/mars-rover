public static class CircleAIParams{    
    // Algorithmic movement parameters
    public static readonly float MAX_RADIUS = 1000f;          // Maximum radius in meters
    public static readonly float RADIUS_INCREMENT = 0.1f;    // Increment as percentage of maxRadius
    public static readonly float CIRCLE_SPEED = 0.1f;        // Speed of circular movement (lower = slower)
}